name: Continuous Deployment
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  build:
    name: Build Docker image
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4
      - name: Build
        run: docker compose build explorer
